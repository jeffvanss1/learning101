{
  "name": "watchparty-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A modern, YouTube-inspired watch party web app built on Cloudflare Workers, Static Assets, and Durable Objects.",
  "scripts": {
    "dev": "wrangler dev",
    "dev:local": "wrangler dev --ip 0.0.0.0 --port 8787",
    "deploy": "wrangler deploy",
    "check": "tsc --noEmit && tsc --noEmit -p tsconfig.frontend.json && node --check src/WatchRoom.js && node --check src/anilist.js && node --check dist/js/utils.js && node --check dist/js/api.js && node --check dist/js/player.js && node --check dist/js/catalog.js && node --check dist/js/social.js && node --check dist/js/types.js && node --check dist/js/app.js",
    "test": "node --test tests/*.test.mjs",
    "db:migrate:local": "wrangler d1 migrations apply watchparty-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply watchparty-db --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260911.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.131.1"
  }
}
